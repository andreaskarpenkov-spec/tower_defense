# Python Tower Defense

A simple tower defense game built with `pygame`.

## Run

1. Install dependencies:

- Note: you do not need to install dependencies if you have pygame>=2.0

```bash
pip install -r requirements.txt
```

2. Run the game:

```bash
python3 main.py
```

## Controls

- Left-click to place a tower.
- Press `R` to restart after game over.
- unlock new towers
- can you get the secret tower?

## Goal

Stop enemies from reaching the end of the path using towers placed along the route.
